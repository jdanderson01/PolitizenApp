# PolitizenApp

```bash
# Go into the repository
$ cd PolitizenMobile

# Install dependencies
$ npm install

# Open Simulator app (XCode for iOs)

# Run the app
$ npx expo start
$ press i for iOs simulator
```
